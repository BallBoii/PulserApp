# PulseBlaster Python Dependencies
# These are the required packages for the PulseBlaster Python wrapper

# SpinCore SpinAPI Python bindings
# Note: This typically needs to be installed from the SpinCore SDK
# or from a local wheel file provided by SpinCore
# Core dependencies for PulseBlaster Python wrapper
spinapi==3.2.2

# For development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Data handling (if needed for extended functionality)
numpy>=1.20.0
matplotlib>=3.5.0

# Async support (if adding async functionality)
asyncio-mqtt>=0.11.0

# Development tools
black>=22.0.0
flake8>=4.0.0
mypy>=0.910